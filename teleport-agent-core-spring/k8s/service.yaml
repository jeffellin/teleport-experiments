apiVersion: v1
kind: Service
metadata:
  name: spring-gateway
  namespace: default
  labels:
    app: spring-gateway
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: spring-gateway